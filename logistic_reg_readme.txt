First, I have read the file with command  "data = csvread ('digits.csv');"

You can acccess all the results on "main.m" which is calling all other optimization, prediction and activation functions.
 

